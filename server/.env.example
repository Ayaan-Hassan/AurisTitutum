# =============================================================
# AurisTitutum PRO — Backend Server Environment Variables
# Copy this file to .env for local development
# Set these as Environment Variables in your Render dashboard
# =============================================================

# ── Server ───────────────────────────────────────────────────
# Render sets PORT automatically — do NOT override it on Render
# For local development, 3001 is the default
PORT=3001

# ── CORS — Frontend Origin ───────────────────────────────────
# Local development: http://localhost:5173
# Production (Vercel): https://your-app-name.vercel.app
FRONTEND_URL=http://localhost:5173

# ── Google OAuth 2.0 ─────────────────────────────────────────
# Get these from: Google Cloud Console → APIs & Services → Credentials
# Create an OAuth 2.0 Client ID of type "Web application"
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# ── Google OAuth Redirect URI ─────────────────────────────────
# This MUST match exactly what you set in Google Cloud Console
# under "Authorized redirect URIs" for your OAuth client.
#
# Local development:
#   http://localhost:3001/auth/google/callback
#
# Production (Render) — replace with your actual Render service URL:
#   https://your-backend-name.onrender.com/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

# ── Node Environment ─────────────────────────────────────────
# Set to "production" on Render
NODE_ENV=development
