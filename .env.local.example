# ─────────────────────────────────────────────────────────────────────────────
# AurisTitutum PRO — Local Development Environment Variables
#
# 1. Copy this file:   cp .env.local.example .env.local
# 2. Fill in every value marked ✏️  below
# 3. NEVER commit .env.local — it is already in .gitignore
#
# For production, set these same variables in:
#   Vercel Dashboard → Your Project → Settings → Environment Variables
# ─────────────────────────────────────────────────────────────────────────────


# ── Firebase (Frontend — MUST be prefixed VITE_) ─────────────────────────────
# Get these from: Firebase Console → Project Settings → Your Apps → Web App
# https://console.firebase.google.com

VITE_FIREBASE_API_KEY=AIzaSy...                         # ✏️  Web API Key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com  # ✏️
VITE_FIREBASE_PROJECT_ID=your-project-id                # ✏️
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com   # ✏️
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012          # ✏️
VITE_FIREBASE_APP_ID=1:123456789012:web:abc123def456    # ✏️


# ── Backend API base URL (Frontend) ──────────────────────────────────────────
# Leave EMPTY for production and `vercel dev` — API is same-origin at /api.
# Only set this if you are running the frontend (npm run dev) separately from
# the API (i.e. NOT using `vercel dev`).
#
# Example for split dev setup:  VITE_BACKEND_URL=http://localhost:3000

VITE_BACKEND_URL=


# ── Google OAuth 2.0 (Server-side — DO NOT prefix with VITE_) ────────────────
# Get these from: Google Cloud Console → APIs & Services → Credentials
# https://console.cloud.google.com
#
# ⚠️  These are SERVER SECRETS — never expose them to the browser.
# ⚠️  GOOGLE_REDIRECT_URI must be registered EXACTLY as shown in Google Cloud
#     Console under "Authorized redirect URIs" for your OAuth 2.0 Client ID.

GOOGLE_CLIENT_ID=123456789012-abcdefghijklmnopqrstuvwxyz.apps.googleusercontent.com  # ✏️
GOOGLE_CLIENT_SECRET=GOCSPX-...                                                       # ✏️

# For local development with `vercel dev` (default port 3000):
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# For production, change this to your Vercel deployment URL, e.g.:
# GOOGLE_REDIRECT_URI=https://your-app.vercel.app/api/auth/google/callback


# ── Frontend origin (Server-side — used by OAuth callback) ───────────────────
# The URL the OAuth callback redirects back to after Google consent.
# Leave empty in production — the callback derives it from the request host.
#
# For local development with `vercel dev`:
FRONTEND_URL=http://localhost:3000

# For local development with `npm run dev` (Vite only, port 5173):
# FRONTEND_URL=http://localhost:5173


# ── Upstash Redis (Server-side — persistent OAuth token store) ───────────────
# Get these from: https://upstash.com → Your Database → REST API
# OR via Vercel Marketplace: Vercel Dashboard → Integrations → Upstash Redis
#   (the integration adds these automatically to your Vercel project)
#
# Without Redis, the store falls back to an in-memory Map that is wiped on
# every cold start — fine for local testing, NOT suitable for production.

UPSTASH_REDIS_REST_URL=https://your-db.upstash.io       # ✏️
UPSTASH_REDIS_REST_TOKEN=AX...                          # ✏️
